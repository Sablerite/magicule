[package]
name = "proton"
version = "0.1.0"
edition = "2021"
description = "A Rust library for converting inches to meters with CLI interface"
authors = ["Sablerite"]
repository = "https://github.com/sablerite/proton" # Placeholder URL

[dependencies]
rand = "0.10.2"
thiserror = "1.0"
anyhow = "1.0"

[dev-dependencies]
criterion = "0.5"
